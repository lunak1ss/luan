# Netlify 部署说明（修复后）

本项目是 Next.js 16 应用，部署到 Netlify 时按以下步骤操作：

## 1. 更新 GitHub 仓库

把修复后的项目内容替换到你的 GitHub 仓库（保留 `.git` 和 `.github` 等已有文件）：

```bash
git add -A
git commit -m "修复图片文件名、PDF 忽略规则和数据接口"
git push
```

> 注意：`public/images/` 里的图片已按代码引用的中文名重命名；
> 请确认仓库里现在是修复后的文件，而不是旧的“下划线文件名”。

## 2. Netlify 配置

在 Netlify（Build & deploy → Environment / Build settings）确认：

- **Framework preset**：Next.js（让 Netlify 自动安装 Next.js runtime 插件，图片优化和 API 路由依赖它）
- **Build command**：`pnpm build`（或 `npm run build`；两者都会先安装依赖）
- **Publish directory**：留空或按插件提示填 `.next`
- **Node version**：20 或更高（Next.js 16 要求）

如果之前构建失败过，可以在 Deploys 页面点 **Trigger deploy / Clear cache and deploy site** 重新部署。

## 3. 部署后检查

- 打开页面按 F12 → Network，确认没有 404 的图片请求
- 项目详情里的图片、海报、简历下载应正常显示/下载

## 4. 文件情况

PDF（3 个“为你撑伞”文档 + 简历）已放入 `public/pdfs/`，见 `README-已添加的PDF.md`。
图片已全部齐备（含从 PDF 截出的 4 张项目详情图），页面引用的资源无缺失。
